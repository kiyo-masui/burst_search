"""Search DM space data for events."""


def basic(dm_data):
    """Simple event search of DM data.

    Returns
    -------
    triggers : list of tuple pairs of intergers
        First interger is the index along the DM axis, the second is the index
        along the time axis.

    """

    triggers = []

    return triggers
